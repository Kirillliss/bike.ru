.index_as_sortable
  %h3
    Страницы
    .btn-group.btn-group-xs.pull-right
      = link_to 'Добавить', new_admin_page_path, class: 'btn btn-primary'

  %ol#categories.ui-sortable{ data: { 'start-collapsed' => 'false', 'max-levels' => 2, sortable: { type: 'tree', url: "#{sort_admin_pages_path}" }}}
    = render partial: 'page', collection: Page.roots.order(:position)